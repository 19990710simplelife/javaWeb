<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.simplelife.dao.IProductDAO">
    <sql id="sql_where">
        <where>
            <if test="keyWord!=null and keyWord!=''">
                (productName LIKE concat('%',#{keyWord},'%') OR brand LIKE concat('%',#{keyWord},'%'))
            </if>
            <if test="productName!=null and productName !=''">
                AND productName LIKE concat('%',#{productName},'%')
            </if>
            <if test="minSalePrice!=null">
                AND salePrice &gt;= #{minSalePrice}
            </if>
            <if test="maxSalePrice!=null">
                AND salePrice &lt;= #{maxSalePrice}
            </if>
        </where>
    </sql>

    <insert id="insert">
        INSERT INTO product(productName, dirId, salePrice, supplier, brand, cutoff, costPrice)
        VALUES (#{productName}, #{dirId}, #{salePrice}, #{supplier}, #{brand}, #{cutoff}, #{costPrice})
    </insert>

    <delete id="delete">
        DELETE
        FROM product
        WHERE id = #{id}
    </delete>

    <update id="update">
        UPDATE product
        SET productName=#{productName},
            dirId=#{dirId},
            salePrice=#{salePrice},
            supplier=#{supplier},
            brand=#{brand},
            cutoff=#{cutoff},
            costPrice=#{costPrice}
        WHERE id = #{id}
    </update>

    <select id="selectOnes" resultType="cn.simplelife.domain.Product">
        SELECT *
        FROM product
        WHERE id = #{id}
    </select>

    <select id="selectAlls" resultType="cn.simplelife.domain.Product">
        SELECT *
        FROM product
    </select>

    <select id="selectForList" resultType="cn.simplelife.domain.Product">
        SELECT *
        FROM product
        <include refid="sql_where"/>
        LIMIT #{beginIndex},#{pageSize}
    </select>

    <select id="selectForCount" resultType="int">
        SELECT COUNT(*)
        FROM product
        <include refid="sql_where"/>
    </select>
</mapper>